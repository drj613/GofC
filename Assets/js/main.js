$(document).ready(function(){
    
    var vertCarriage = new Image();
    vertCarriage.src = '../img/vertCarriage.png';

    function sprite(){
        var that = {};

        that.context = options.context;
        that.width = options.width;
        that.height = options.height;
        that.image = options.image;

        return that;
    }

    var $carriageCanvas = $("animTest");
    $carriageCanvas.width = 

    
});
